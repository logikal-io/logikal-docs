name: Publish release
on:
  release:
    types: [released]

jobs:
  publish-release:
    name: Publish release
    uses: logikal-io/github-workflows/.github/workflows/publish-release.yml@v1.1.1-dev11
    with:
      name: logikal-docs
    permissions:
      contents: read
      id-token: write
    secrets: inherit
